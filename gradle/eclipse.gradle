apply plugin: "eclipse"

tasks.eclipse.dependsOn(tasks.cleanEclipse)
//tasks.eclipse.dependsOn(tasks.compileJava)

eclipse {
    jdt {
        sourceCompatibility = javaVersion
        targetCompatibility = javaVersion
    }
}
